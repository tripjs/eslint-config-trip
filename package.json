{
  "name": "eslint-config-trip",
  "description": "ESLint rules for trip.js and related modules",
  "version": "0.0.0",
  "devDependencies": {
    "js-yaml": "^3.4.5"
  },
  "files": [
    "index.js",
    "config.json"
  ],
  "license": "MIT",
  "repository": "tripjs/eslint-config-trip.git",
  "scripts": {
    "prepublish": "js-yaml config.yaml > config.json",
    "test": "node index"
  }
}
